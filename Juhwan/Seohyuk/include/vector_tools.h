
#pragma once

#include <string>
#include <vector>
#include <memory>

namespace vectool {
    namespace transform {
        std::vector<char * const> strVecToCharArrUnique(const std::vector<std::string> &strVec) {
            //TODO: Function that transforms string vector to char * const array with unique pointer
        }
    }
}
